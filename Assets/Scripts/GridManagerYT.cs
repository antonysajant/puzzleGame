using JetBrains.Annotations;
using UnityEngine;

public class GridManagerYT : MonoBehaviour
{
    [SerializeField] int rows, cols;
    [SerializeField] GameObject tile;
    void generateGrid()
    {
        for (int i = 0; i < cols; i++)
        {
            for (int j = 0; j < rows;j++)
            { 
            }
        }
    }
}
